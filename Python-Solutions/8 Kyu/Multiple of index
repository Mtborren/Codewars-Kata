https://www.codewars.com/kata/5a34b80155519e1a00000009/train/python
8

    def multiple_of_index(arr):
        ind = list(enumerate(arr))
        newarr = []
        for x in ind:
            print(x[0], x[1])
            if x[0] > 0 and x[1] % x[0] == 0:
                newarr.append(x[1])
        return newarr
