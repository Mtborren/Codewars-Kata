https://www.codewars.com/kata/57cebe1dc6fdc20c57000ac9/train/python

    def find_short(s):
        strings = s.split(" ")
        sortedstrings = sorted(strings, key=len)
        smallest = sortedstrings[0]
        return len(smallest)
