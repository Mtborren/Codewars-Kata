https://www.codewars.com/kata/554b4ac871d6813a03000035/train/python

    def high_and_low(numbers):
        numbers = numbers.split(" ")
        lst = [int(nums) for nums in numbers]
        lst.sort()
        high = lst[len(lst) - 1]
        low = lst[0]
        highlow = str(high) + " " + str(low)
        return highlow
