https://www.codewars.com/kata/5842df8ccbd22792a4000245/train/python

    def alphabet_position(text):
        text = text.lower()
        newseq = []
        for lets in text:
            lets = str(ord(lets) - 96)
            if lets > '0':
                newseq.append(lets)
        return " ".join(newseq)
